services:
  - type: web
    name: face-color-project
    env: python
    buildCommand: apt-get install -y libgles2 libgl1 && pip install -r backend/requirements.txt
    startCommand: cd backend && uvicorn main:app --host 0.0.0.0 --port $PORT
    rootDir: .